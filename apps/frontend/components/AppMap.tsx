// Web stub — react-native-maps is not supported on web
import { View } from 'react-native';

export function AppMap({ style }: { style?: any; [key: string]: any }) {
  return <View style={[{ backgroundColor: '#e8eaf0', borderRadius: 20 }, style]} />;
}
